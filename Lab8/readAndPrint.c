
#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <unistd.h>

int main(){

    inputBuffer = // add the input to the buffer which comes from the std output of the other file
    while (fgets(inputBuffer, 100, stdin)){
            int page_num = atoi(pageCache);
            /*
            Page Replacement Implementation Goes Here
            */

        printf("%d\n", page_num);
        }

}